namespace Vcs.Git;

/// <summary>
/// Placeholder type so the project compiles and tests pass out of the box.
/// Replace it with the Git CLI automation API.
/// </summary>
public static class GitCli
{
	/// <summary>Name of the underlying command-line executable this client drives.</summary>
	public static string ExecutableName => "git";
}
